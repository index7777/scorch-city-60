const CDP='http://127.0.0.1:9222',APP='http://127.0.0.1:4173/';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function assert(ok,msg){if(!ok)throw new Error(msg)}async function waitFor(fn,timeout=20000,step=100){const end=Date.now()+timeout;while(Date.now()<end){try{const v=await fn();if(v)return v}catch{}await sleep(step)}throw new Error('timeout')}
const target=await waitFor(async()=>{const r=await fetch(`${CDP}/json/list`);if(!r.ok)return null;const list=await r.json();return list.find(x=>x.type==='page'&&x.url.startsWith(APP))||list.find(x=>x.type==='page')});assert(target?.webSocketDebuggerUrl,'no Chrome page target');const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{ws.addEventListener('open',resolve,{once:true});ws.addEventListener('error',reject,{once:true})});let seq=0;const pending=new Map();ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});function send(method,params={}){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))})}async function evaluate(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text||'evaluate exception');return r.result?.value}
await send('Runtime.enable');await waitFor(()=>evaluate(`typeof qaStateV84==='function'&&typeof mergeSave==='function'&&typeof residentReadModelV110==='function'&&window.__SCORCH_ENTRY_ACTIVE===false`),30000);
const result=await evaluate(`(()=>{const legacy=JSON.parse(JSON.stringify(qaStateV84()));legacy.day=18;legacy.phase='night';legacy.hoursLeft=4;delete legacy.player;delete legacy.knowledge;delete legacy.waterAllocation;delete legacy.electricity;delete legacy.residentClock;delete legacy.residentSurvivalClock;mergeSave(legacy);const r=residentReadModelV110();return {day:r.day,player:state.player,knowledge:state.knowledge,water:state.waterAllocation,electricity:state.electricity,read:r};})()`);
assert(result.day===18,'legacy day was not preserved');assert(result.player&&Number.isFinite(result.player.hydration)&&Number.isFinite(result.player.bodyTemp),'resident player state was not migrated');assert(result.knowledge&&Array.isArray(result.knowledge.contacts),'knowledge ledger was not migrated');assert(result.water&&Number.isFinite(result.water.drinkingPool)&&Number.isFinite(result.water.coolingPool),'water allocation was not migrated');assert(result.electricity&&Number.isFinite(result.electricity.batteryKWh),'electricity state was not migrated');assert(result.read.water.drinkingL+result.read.water.coolingL<=result.read.water.totalL+.001,'migrated water pools exceed physical water');console.log('PASS resident legacy save migration regression');ws.close();
