const CDP='http://127.0.0.1:9222',APP='http://127.0.0.1:4173/';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function assert(ok,msg){if(!ok)throw new Error(msg)}
async function waitFor(fn,timeout=20000,step=100){const end=Date.now()+timeout;while(Date.now()<end){try{const v=await fn();if(v)return v}catch{}await sleep(step)}throw new Error('timeout')}
const target=await waitFor(async()=>{const r=await fetch(`${CDP}/json/list`);if(!r.ok)return null;const list=await r.json();return list.find(x=>x.type==='page'&&x.url.startsWith(APP))||list.find(x=>x.type==='page')});assert(target?.webSocketDebuggerUrl,'no Chrome page target');
const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{ws.addEventListener('open',resolve,{once:true});ws.addEventListener('error',reject,{once:true})});let seq=0;const pending=new Map();ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});function send(method,params={}){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))})}async function evaluate(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text||'evaluate exception');return r.result?.value}
await send('Runtime.enable');await waitFor(()=>evaluate(`typeof qaStateV84==='function'&&typeof residentReadModelV110==='function'&&typeof dayTemp==='function'&&typeof nightHours==='function'&&window.__SCORCH_ENTRY_ACTIVE===false`),30000);
const result=await evaluate(`(()=>{window.state=qaStateV84();state.resources.water=999;state.resources.food=999;state.resources.battery=999;state.resources.fuel=999;state.base.core=true;state.knownCore=true;const seen=[];for(let d=1;d<=60;d++){state.day=d;state.phase=d<30?'night':'day';state.hoursLeft=d<30?nightHours(d):0;state.player={hydration:100,satiety:100,stamina:100,health:100,bodyTemp:36.5,heat:0,dead:false};state.residentClock={day:d,phase:state.phase,hoursLeft:state.hoursLeft};state.residentSurvivalClock={day:d,phase:state.phase,hoursLeft:state.hoursLeft};render();const r=residentReadModelV110();if(r.day!==d)throw new Error('read model day drift '+d);if(dayTemp(d)!==(d<8?72:d<15?78:d<22?84:d<30?92:100))throw new Error('temperature schedule drift '+d);if(nightHours(d)!==(d<8?8:d<15?6:d<22?4:d<30?2:0))throw new Error('night schedule drift '+d);if(d===29||d===30||d===60)seen.push({d,temp:dayTemp(d),night:nightHours(d),dead:!!state.player.dead});}return {day:state.day,seen,boot:window.__SCORCH_BOOT_ERROR||''};})()`);
assert(result.day===60,'long path did not reach Day 60');assert(result.seen.length===3,'boundary days were not exercised');assert(result.seen[0].night===2&&result.seen[1].night===0&&result.seen[1].temp===100,'Day 29 to 30 boundary is wrong');assert(result.seen.every(x=>!x.dead),'safe long-path fixture died unexpectedly');assert(!result.boot,'runtime boot error during long path');console.log('PASS resident Day 1-60 long-path and Day 29-30 boundary regression');ws.close();
