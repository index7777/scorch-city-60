const CDP='http://127.0.0.1:9222',APP='http://127.0.0.1:4173/';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function assert(ok,msg){if(!ok)throw new Error(msg)}async function waitFor(fn,timeout=20000,step=100){const end=Date.now()+timeout;while(Date.now()<end){try{const v=await fn();if(v)return v}catch{}await sleep(step)}throw new Error('timeout')}
const target=await waitFor(async()=>{const r=await fetch(`${CDP}/json/list`);if(!r.ok)return null;const list=await r.json();return list.find(x=>x.type==='page'&&x.url.startsWith(APP))||list.find(x=>x.type==='page')});assert(target?.webSocketDebuggerUrl,'no Chrome page target');const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{ws.addEventListener('open',resolve,{once:true});ws.addEventListener('error',reject,{once:true})});let seq=0;const pending=new Map();ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});function send(method,params={}){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))})}async function evaluate(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text||'evaluate exception');return r.result?.value}
await send('Runtime.enable');await waitFor(()=>evaluate(`typeof qaStateV84==='function'&&typeof applyZeroResourceOpeningV112==='function'&&document.getElementById('demoStart')&&!document.getElementById('demoStart').disabled`),30000);
const broadcast=await evaluate(`document.getElementById('demoHowToPanel')?.textContent||''`);assert(/Day 1–7/.test(broadcast)&&/Day 30/.test(broadcast)&&/100°C/.test(broadcast),'broadcast schedule missing');assert(!/Day 60|D60|期限/.test(broadcast),'broadcast leaks Day 60');
await evaluate(`document.getElementById('demoStart').click()`);await waitFor(()=>evaluate(`qaStateV84().flags?.hardFogOpeningV112===true&&document.querySelector('.hard-fog-shelter-v112')`));
const result=await evaluate(`(()=>{const s=qaStateV84();return {resources:s.resources,backpack:s.backpack,shelterStorage:s.shelterStorage,shelterPower:s.shelterPower,left:document.querySelector('.left-panel')?.innerText||'',bottomHidden:document.querySelector('.bottom-strip')?.hidden===true,mapToolsHidden:document.querySelector('.map-tools')?.hidden===true,plannerHidden:document.getElementById('mapPlannerPanel')?.hidden===true,deckHidden:document.querySelector('.command-deck')?.hidden===true,daysHidden:document.getElementById('daysLeft')?.closest('.stat')?.hidden===true,visibleText:document.body?.innerText||''};})()`);
for(const [k,v] of Object.entries(result.resources||{}))assert(Number(v)===0,`opening resource ${k} is not zero`);assert(result.backpack?.capacityKg===50&&result.backpack?.singleItemLimitKg===20,'backpack limits incorrect');assert(result.shelterStorage?.capacityKg===200,'shelter storage limit incorrect');assert(result.shelterPower?.outlets===1,'shelter outlet count incorrect');assert(/背包 0\.0 \/ 50 kg/.test(result.left),'backpack HUD missing');assert(/儲放空間 0\.0 \/ 200 kg/.test(result.left),'shelter storage HUD missing');assert(/充電插座 1 個/.test(result.left),'shelter outlet HUD missing');for(const leak of ['已知城市水源','已發現大型物件','外部冷站','中央通風站','大型資產','研究資料','已知倖存人口','中央站容量','路線規劃'])assert(!result.left.includes(leak),`left HUD leaks ${leak}`);assert(result.bottomHidden&&result.mapToolsHidden&&result.plannerHidden&&result.deckHidden&&result.daysHidden,'future-system shell not hidden');assert(!/你不是全知的城市管理者/.test(result.visibleText),'unnecessary god-view explanation still visible');console.log('PASS resident hard-fog opening regression');ws.close();
