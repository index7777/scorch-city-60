const CDP='http://127.0.0.1:9222',APP='http://127.0.0.1:4173/';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function assert(ok,msg){if(!ok)throw new Error(msg)}async function waitFor(fn,timeout=20000,step=100){const end=Date.now()+timeout;while(Date.now()<end){try{const v=await fn();if(v)return v}catch{}await sleep(step)}throw new Error('timeout')}
const target=await waitFor(async()=>{const r=await fetch(`${CDP}/json/list`);if(!r.ok)return null;const list=await r.json();return list.find(x=>x.type==='page'&&x.url.startsWith(APP))||list.find(x=>x.type==='page')});assert(target?.webSocketDebuggerUrl,'no Chrome page target');const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{ws.addEventListener('open',resolve,{once:true});ws.addEventListener('error',reject,{once:true})});let seq=0;const pending=new Map();ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});function send(method,params={}){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))})}async function evaluate(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text||'evaluate exception');return r.result?.value}
await send('Runtime.enable');await waitFor(()=>evaluate(`typeof ensureHiddenPopulationV114==='function'&&typeof killHiddenPersonV114==='function'&&document.getElementById('demoStart')&&!document.getElementById('demoStart').disabled`),30000);
await evaluate(`document.getElementById('demoStart').click()`);await waitFor(()=>evaluate(`window.__SCORCH_ENTRY_ACTIVE===false`));
const result=await evaluate(`(()=>{const s=qaStateV84();window.state=s;applyZeroResourceOpeningV112(s);delete s.hiddenPopulationV114;const hp=ensureHiddenPopulationV114(s);const hidden=hp.people.find(p=>!p.player&&!p.discovered);const beforeLog=s.log.length;const killed=killHiddenPersonV114(hidden.id,'QA heat',s);const afterLog=s.log.length;const beforeKnown=hp.knownIds.length;const found=revealPeopleAtV114(hidden.location,s);render();const visible=document.body.innerText||'';return {total:hp.people.length,declared:hp.total,player:hp.people.filter(p=>p.player).length,initialKnown:beforeKnown,killed,deathSilent:beforeLog===afterLog,revealed:hp.knownIds.includes(hidden.id),corpseSeen:hp.corpseSeenIds.includes(hidden.id),foundCorpse:found.corpses.includes(hidden.id),noGlobalTotal:!/(世界人口|總人口|50\s*人)/.test(visible),bottomHidden:document.querySelector('.bottom-strip')?.hidden===true};})()`);
assert(result.total===50&&result.declared===50,'world population is not exactly 50');assert(result.player===1,'player should be one of the 50 people');assert(result.initialKnown===1,'opening should only know self');assert(result.killed&&result.deathSilent,'unknown death was not silent');assert(result.revealed&&result.corpseSeen&&result.foundCorpse,'corpse encounter did not reveal prior existence');assert(result.noGlobalTotal,'player-facing UI leaked world population total');assert(result.bottomHidden,'legacy population HUD became visible');console.log('PASS resident hidden population regression');ws.close();