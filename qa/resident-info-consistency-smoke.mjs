const CDP='http://127.0.0.1:9222',APP='http://127.0.0.1:4173/';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));function assert(ok,msg){if(!ok)throw new Error(msg)}async function waitFor(fn,timeout=20000,step=100){const end=Date.now()+timeout;while(Date.now()<end){try{const v=await fn();if(v)return v}catch{}await sleep(step)}throw new Error('timeout')}
const target=await waitFor(async()=>{const r=await fetch(`${CDP}/json/list`);if(!r.ok)return null;const list=await r.json();return list.find(x=>x.type==='page'&&x.url.startsWith(APP))||list.find(x=>x.type==='page')});assert(target?.webSocketDebuggerUrl,'no Chrome page target');const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{ws.addEventListener('open',resolve,{once:true});ws.addEventListener('error',reject,{once:true})});let seq=0;const pending=new Map();ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});function send(method,params={}){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))})}async function evaluate(expression){const r=await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text||'evaluate exception');return r.result?.value}
await send('Runtime.enable');await waitFor(()=>evaluate(`typeof qaStateV84==='function'&&typeof residentReadModelV110==='function'&&typeof residentUiRefreshV110==='function'&&window.__SCORCH_ENTRY_ACTIVE===false`),30000);
const result=await evaluate(`(()=>{window.state=qaStateV84();state.day=30;state.phase='day';state.hoursLeft=0;state.resources.water=20;state.resources.fuel=17;state.waterAllocation={schema:1,drinkingPool:14,coolingPool:6,emergencyCoolingFromDrinking:false};state.electricity={batteryKWh:1.5,capacityKWh:4,shelterOutputKW:.25,tools:{fan:{name:'測試風扇',chargeKWh:.3,capacityKWh:.5,drawKW:.1,maxChargeKW:.25}}};state.gear=state.gear||{};state.gear.vehicle=true;state.logistics=state.logistics||{};state.logistics.heavyReady=false;state.vehicle=state.vehicle||{};state.vehicle.hasAC=true;state.vehicle.acActive=true;render();residentUiRefreshV110();const read=residentReadModelV110();const waterText=document.querySelector('#residentHud .water-truth-v110')?.textContent||'';const dlg=document.getElementById('electricityDialog');if(dlg&&!dlg.open)dlg.showModal();renderAuthoritativeElectricityV110();const electricityText=document.getElementById('electricityContent')?.textContent||'';if(dlg?.open)dlg.close();if(typeof renderAuthoritativeVehiclePanelV107==='function')renderAuthoritativeVehiclePanelV107();const vehicleText=document.getElementById('vehicleContent')?.textContent||'';decorateKnowledgeSourceIconsV109(document);const sourceMarks=[...document.querySelectorAll('#residentHud .source-label')];const iconCount=sourceMarks.filter(x=>x.querySelector('img.knowledge-source-icon-v109')).length;return {read,waterText,electricityText,vehicleText,sourceCount:sourceMarks.length,iconCount};})()`);
assert(Math.abs(result.read.water.drinkingL-14)<.01&&Math.abs(result.read.water.coolingL-6)<.01,'water read model drifted from authoritative allocation');assert(result.waterText.includes('飲用水可撐')&&result.waterText.includes('14.0L')&&result.waterText.includes('6.0L'),'resident HUD is not showing authoritative drinking/cooling pools');assert(result.electricityText.includes('1.50 kWh')&&result.electricityText.includes('0.30 / 0.50 kWh'),'electricity panel is not reading authoritative bank/tool batteries');assert(result.read.transport.id==='car'&&result.vehicleText.includes(result.read.transport.name),'vehicle panel disagrees with authoritative transport model');assert(result.sourceCount>0&&result.iconCount===result.sourceCount,'resident source labels are not consistently decorated with formal icons');console.log('PASS resident information consistency regression');ws.close();
